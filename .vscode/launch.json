{
    "configurations": [
        {
            "name": "Cold Load Pickup MPC",
            "type": "debugpy",
            "request": "launch",
            "python": "${workspaceFolder}/cold-pickup-mpc/.venv/Scripts/python",
            "module": "cold_pickup_mpc.app",
            "cwd": "${workspaceFolder}/cold-pickup-mpc",
            "envFile": "${workspaceFolder}/local-dev.env"
        }
   ],
}